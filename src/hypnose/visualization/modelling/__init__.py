"""Figures for the behavioural sequence models in ``hypnose.modelling``.

One subpackage per analysis, mirroring ``hypnose.modelling``. Figures depend on the model
results (and on ``hypnose.modelling`` for the fitted curves); the model code never depends on
these, so the dependency runs one way, visualization -> modelling.
"""
